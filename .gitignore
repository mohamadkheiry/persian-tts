.gradle/
build/
app/build/
local.properties
*.log
.idea/
.DS_Store
dl/

# مدل‌ها (صداهای Piper، مدل‌های شبیه‌سازی صدا، Whisper، Gemma و...) عمداً commit نمی‌شوند — حجم
# زیاد (چند گیگابایت) و سقف رایگان Git LFS گیت‌هاب دلیل اصلی است. روش کامل بازسازی/دانلود هر مدل
# در بخش «تهیه‌ی مدل‌ها» CLAUDE.md و README مستند شده. این پوشه فقط محلی/برای build است.
models/

# نسخه‌های bundle‌شده‌ی مدل‌ها برای بیلد محلیِ «کاملاً آفلاین/یک‌فایلی» (بدون دانلود در زمان اجرا).
# روش ساختشان (کپی از models/) در بخش «بیلد کاملاً بسته‌بندی‌شده» CLAUDE.md مستند شده.
app/src/main/assets/ganji/
app/src/main/assets/gyro/
app/src/main/assets/amir/
app/src/main/assets/ganji_adabi/
app/src/main/assets/reza_ibrahim/
app/src/main/assets/voice_clone/
app/src/main/assets/stt/

# خروجی APK — هر کسی از سورس build می‌کند، خودش این را می‌سازد؛ نیازی به commit نیست.
dist/*.apk
